#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <string>
#include <vector>

namespace xmlpl {
  std::vector<std::string> tokenize(const std::string &str, const std::string &delimiters);
};
#endif // FUNCTIONS_H
